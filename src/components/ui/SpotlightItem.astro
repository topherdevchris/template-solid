---
const { image, title, body, href = "#", reverse = false } = Astro.props;
---

<div class={`grid gap-8 items-center md:grid-cols-2 ${reverse ? "md:[&>div:first-child]:order-2" : ""}`}>
  <div>
    <img src={image} alt="" class="w-full rounded-lg border border-line" loading="lazy" />
  </div>
  <div>
    <h3 class="text-2xl font-bold mb-3">{title}</h3>
    <p class="text-muted mb-4">{body}</p>
    <a href={href} class="inline-block px-5 py-2 rounded-md bg-primary text-white font-medium hover:opacity-90 focus:outline-none focus:ring-2 focus:ring-primary/50">
      Learn more
    </a>
  </div>
</div>
